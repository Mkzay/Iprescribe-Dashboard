# API Configuration
VITE_API_BASE_URL=https://stagingapi.iprescribe.online/api/v1

# Theme Preference (optional)
# Saved automatically in localStorage as 'theme-mode'
# Values: 'light' or 'dark' (defaults to system preference)
